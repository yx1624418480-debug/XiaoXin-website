# DESIGN.md

## 项目与用户画像
- 站点：短视频编导「小星同学TX」的个人品牌站。
- 双重用途：① 自媒体主页（小红书/抖音引流、个人品牌）；② 求职名片（HR/面试官建立认知）。
- 参照站点：xmmxovo.online（西门聪明蛋XD），**1:1 对标其视觉语言**。
- 关键约束：在职状态，不展示公司商业成片/公司名；内容以小红书 AI 科普图文、抖音美食视频、航海与学习经历为主。

## 气质与意象
- **新粗野主义 × 孟菲斯手账涂鸦（Neo-Brutalism × Memphis Doodle）**。
- 意象：像一本贴满彩色便利贴、随手涂鸦、粗黑笔勾边的手账本；卡片像"啪"地贴在桌面上的贴纸，带硬邦邦的黑色偏移投影。
- 情绪：年轻、有网感、真诚、好玩、不端着；弱化"职场简历感"，突出个人魅力与行动感。

## 视觉策略
- 关键词高亮：人名、关键词用高饱和色块"马克笔高亮"（粉/蓝/绿/黄）。
- 图形语言：粗黑描边、硬投影（无模糊）、手绘装饰（星星 ✦、箭头 →、闪电 ⚡、虚线、小圆点）。
- 游戏化/贴纸元素：ID CARD 身份证、"地球Online 开放游戏进度"时间线（主线/支线任务）、"近日生活"卡片、圆形笑脸贴纸、条形码。
- 封面：文章/视频暂用高饱和彩色色块 + 涂鸦/emoji 占位（后续可换真实截图，需保持粗描边圆角卡片外框）。

## 配色方案（Design Tokens）
- 背景：奶白/米白 `#F4F3EF`（不是纯白）。
- 墨色（描边/正文/投影）：近黑 `#111111`。
- 高饱和点缀色：
  - 粉 `#F9A8D4`（人名高亮、粉色贴纸、平台标签）
  - 蓝 `#93C5FD`（时长高亮、蓝色贴纸、链接）
  - 绿 `#86EFAC`（ID CARD 卡头、The Journal 高亮、支线）
  - 黄 `#FDE047`（slogan 横幅、Vibe Workshop、按钮）
  - 紫 `#D8B4FE`（JOB 卡等次要块）
- 页脚：纯黑 `#0A0A0A`，白/灰文字。

## 边框与阴影（核心规则）
- 描边：卡片/按钮/输入框统一 `2.5~3px solid #111`。
- 硬投影：`box-shadow: 4px 4px 0 0 #111`（无模糊）；悬停时投影加深/位移变大 `6px 6px 0 #111` 并轻微 `translate`。
- 圆角：大卡片 `rounded-2xl/3xl`（参考站卡片较圆）；按钮/小标签 `rounded-lg/full`。
- 交互反馈：hover 时元素"抬起"（translate 负向 + 投影增大），过渡用短促弹性 `cubic-bezier`，约 150ms。

## 字体排版
- 风格取向：干净、轻、舒展的现代无衬线，**不要**特粗海报体（不用 800/900/Black/ExtraBold）。
- 中文：Noto Sans SC / 苹方 PingFang SC；中文主标题最高 700（font-bold），正文 400/500；中文不加额外字距。
- 英文/Display：Inter（现代无衬线），主标题用 **700 标准粗体**（Inter Bold，厚实但字形开阔不压缩），**禁止** Archivo Black 等超粗/紧排海报体；正文/标签英文用 400/500。用于 "A Short Video Director"、"The Journal"、"Video Library"、"Welcome to"、"ID CARD"、页脚大字等。
- 英文字距：标题不额外加字距（letter-spacing: 0，Inter 本身舒展）；正文小字可 0.01em；英文标题行 `white-space: nowrap` 不换行。
- 通过 globals.css 顶部 `@import` Google Fonts（CN 环境用 `fonts.googleapis.cn`），`.font-display` = Inter 700。
- 关键词高亮色块：粉/蓝贴纸内文字均 700（英文 Inter Bold、中文思源黑体 Bold）；色块内文字上下居中、四边内边距均匀。
- 排版节奏：超大号标题（text-5xl/6xl）+ 关键词色块高亮；正文 16px、行高宽松；中英混排。

## 布局与响应式
- 顶部：居中**胶囊形导航**（黑边 + 硬投影），Tab：首页/关于我/文章/视频，最右邮件图标；当前页 Tab 用黑边方框高亮。
- 4 个 Tab 为独立路由页（/、/about、/articles、/videos）。
- 内容最大宽度约 `max-w-5xl/6xl` 居中，卡片网格：视频 3 列（桌面）→ 1 列（移动）；文章为横向长卡。
- 移动端：导航可横向滚动/缩小，卡片堆叠，字号降级。

## 组件规范
- 所有卡片：白底（或色块）+ 粗黑边 + 硬投影 + 圆角。
- 按钮：黑底白字（主）/黄底黑字（次），粗黑边 + 硬投影。
- 输入框（搜索）：白底粗黑边硬投影，圆角，左侧放大镜图标。
- 标签贴纸：小圆角/胶囊，黑边，粉/蓝/绿/黄底。
- 页脚：黑底，左大字英文 slogan + 中文小字，中 Explore 导航列，右 Connect 社交图标（白底黑边方块），底版权。

## 交互与状态
- 文章/视频页搜索框：前端实时按标题/关键词过滤。
- 外链（小红书/抖音/邮箱）新标签页打开（target=_blank, rel=noopener）。
- 微信图标：点击复制微信号 `XiaoXinTX666`，toast 提示"微信号已复制"。
- 卡片 hover 抬起；时间线节点、贴纸可有轻微浮动/涂鸦动效（克制，不喧宾夺主）。

## 可访问性与文案
- 图片需 alt；图标按钮带 aria-label；对比度足够（黑描边+高饱和色）。
- 文案语气：轻松、有网感、带点自嘲与真诚（"先完成再完美"）。

## 设计禁忌
- 不要科技蓝紫渐变、不要玻璃拟态、不要柔和弥散阴影/模糊光晕。
- 不要直角无描边的"普通后台风"卡片。
- 不要出现公司名/公司商业成片；不虚构粉丝数/播放量等数据。
- 不做"产品"Tab、不做"电子好友"滚动友情链接栏。
- 封面未替换前不要放任何真人/版权图片。
